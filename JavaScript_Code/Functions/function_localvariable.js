function LocalVariables(){
    var carname='volvo';
    console.log(carname)//output shown on command line
    //document.write(carname)//ouput shown on browser
}
LocalVariables()
console.log(carname) // o/p :car name is not defined ,because it is local variable,it scope is only within the function block
